# Быстрая настройка Google Sheets для контактов

## Шаг 1: Создай Google Таблицу

1. Открой https://sheets.google.com
2. Нажми **+ Создать** → **Google Таблицы** (пустая таблица)
3. Назови её: **"Silans Auto Care - Клиенты"**
4. Скопируй **ID таблицы** из адресной строки:
   ```
   https://docs.google.com/spreadsheets/d/1ABC-DEF123XYZ789/edit
                                        ^^^^^^^^^^^^^^^^ это ID
   ```

## Шаг 2: Создай Apps Script

1. Открой https://script.google.com
2. Нажми **+ Новый проект**
3. Удали весь код и вставь содержимое файла `telegram-proxy.gs`
4. Сохрани (Ctrl+S), назови проект: **"Silans Contact Saver"**

## Шаг 3: Добавь ID таблицы и Telegram (опционально)

1. В Apps Script нажми **⚙️ Project Settings** (слева внизу)
2. Прокрути до **Script Properties**
3. Нажми **Add script property** для каждого:
   - **Property:** `SPREADSHEET_ID` → **Value:** `<вставь ID таблицы из шага 1>` *(обязательно)*
   - **Property:** `TELEGRAM_BOT_TOKEN` → **Value:** `<твой токен бота>` *(опционально)*
   - **Property:** `TELEGRAM_CHAT_ID` → **Value:** `<твой chat ID>` *(опционально)*
4. Сохрани

### Как получить Telegram токен и chat ID:
1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot` и следуй инструкциям
3. Скопируй **токен** (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
4. Открой [@userinfobot](https://t.me/userinfobot) и скопируй свой **chat ID** (например: `987654321`)

## Шаг 4: Опубликуй Web App

1. Нажми **Deploy** → **New deployment**
2. Нажми шестерёнку ⚙️ → выбери **Web app**
3. Настройки:
   - **Description:** `v1 - Save to Sheets`
   - **Execute as:** `Me`
   - **Who has access:** `Anyone`
4. Нажми **Deploy**
5. Разреши доступ:
   - **Review permissions** → выбери свой Google аккаунт
   - **Advanced** → **Go to Silans Contact Saver (unsafe)**
   - **Allow**
6. **Скопируй URL** (заканчивается на `/exec`):
   ```
   https://script.google.com/macros/s/AKfycbz.../exec
   ```

## Шаг 5: Добавь URL в сайт

1. Открой файл `src/config.ts`
2. Вставь скопированный URL:
   ```typescript
   export const TELEGRAM_PROXY_URL = 'https://script.google.com/macros/s/AKfycbz.../exec';
   ```
3. Сохрани

## Шаг 6: Протестируй

### Тест 1: Health check
```powershell
Invoke-WebRequest -Uri "https://script.google.com/macros/s/AKfycbz.../exec" -UseBasicParsing | Select-Object Content
```
Должно вернуть: `"ok": true, "configured": true`

### Тест 2: Отправка контакта
```powershell
$url = "https://script.google.com/macros/s/AKfycbz.../exec"
$body = @{ name="Test User"; email="test@test.com"; phone="+1234567890"; message="Hello test" } | ConvertTo-Json
Invoke-WebRequest -Uri $url -Method POST -ContentType "application/json" -Body $body -UseBasicParsing | Select-Object Content
```
Должно вернуть: `"success": true`

### Проверь таблицу
Открой свою Google Таблицу — там должна появиться новая строка с данными тестового контакта.

## Шаг 7: Deploy сайта

```powershell
git add .
git commit -m "feat: add Google Sheets contact logging"
git push origin main
```

Netlify автоматически задеплоит. После деплоя:
- Открой https://silansautocare.com
- Прокрути до формы "Let's Get Started"
- Заполни и отправь
- Проверь Google Таблицу — там появится новая запись

## Готово! 🎉

Теперь каждый контакт с сайта автоматически сохраняется в твою Google Таблицу.

Заходи в таблицу когда удобно и проверяй новых клиентов:
👉 https://docs.google.com/spreadsheets/d/ТВОЙ_ID/edit

---

## Если что-то не работает

### "SPREADSHEET_ID не настроен"
- Проверь Script Properties (шаг 3)
- Убедись что сделал Redeploy после добавления свойства

### "Ошибка при сохранении"
- Проверь что ID таблицы правильный (без лишних символов)
- Убедись что Web App запускается от твоего имени (Execute as: Me)

### Форма на сайте не отправляет
- Открой Developer Tools (F12) → Console — посмотри ошибки
- Проверь что `src/config.ts` содержит правильный URL
- Убедись что сделал `git push` и Netlify задеплоил
